🚀 Yaade has been successfully deployed!

1. Get the application URL:

{{- if .Values.ingress.enabled }}
  {{- range $host := .Values.ingress.hosts }}
    {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
    {{- end }}
  {{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "yaade.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "yaade.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "yaade.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.service.port }}
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "yaade.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:9339 to use Yaade"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 9339:$CONTAINER_PORT
{{- end }}

2. Login with your credentials:

   Username: {{ .Values.yaade.adminUsername }}
   Password: password (⚠️  CHANGE THIS IMMEDIATELY!)

3. IMPORTANT: Change the default password!

   After logging in, go to Settings > Account to change the default password.

4. Optional: Install the browser extension

   For CORS proxy support, install the Yaade browser extension:
   - Chrome: https://chrome.google.com/webstore/detail/yaade/jgmpcefommmbldjnljkjkbhkfgddillg
   - Firefox: https://addons.mozilla.org/en-US/firefox/addon/yaade/

{{- if .Values.persistence.enabled }}

📦 Persistent storage is enabled at /app/data
{{- else }}

⚠️  WARNING: Persistent storage is disabled!
   All data will be lost when the pod restarts.
   Enable persistence in your values.yaml to prevent data loss.
{{- end }}

{{- if .Values.yaade.basePath }}

🔗 Base Path: {{ .Values.yaade.basePath }}
   Yaade is configured to run at a subpath. Make sure your reverse proxy is configured correctly.
{{- end }}

📖 Documentation: https://docs.yaade.io
💬 Community Support: https://github.com/EsperoTech/yaade/discussions
🐛 Report Issues: https://github.com/EsperoTech/yaade/issues
